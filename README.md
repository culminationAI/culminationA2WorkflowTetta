# CulminationA2 Workflow

Multi-agent orchestration framework for Claude Code. Self-aware, self-improving, memory-persistent.

## What It Does

- **4 base agents** with specialized domains (exploration, protocols, engineering, LLM engineering)
- **18 protocols** governing behavior, from task routing to self-improvement
- **Persistent memory** via Qdrant (vector) + Neo4j (graph) — zero API cost
- **Pre-populated self-knowledge** — the coordinator knows its own architecture from day one
- **Adaptive initialization** — 11-phase onboarding that learns your project, style, and preferences
- **Gap detection + self build-up** — identifies capability gaps and builds new agents/protocols as needed

## Quick Start

```bash
# Clone and setup
git clone https://github.com/culminationAI/culminationA2WorkflowTetta.git
cd culminationA2WorkflowTetta
chmod +x setup.sh && ./setup.sh

# Or one-liner:
curl -sL https://raw.githubusercontent.com/culminationAI/culminationA2WorkflowTetta/main/install.sh | bash
```

After setup, open the directory in your IDE and start Claude Code. The workflow detects itself and begins initialization automatically.

## Requirements

- Python 3.9+
- Docker + Docker Compose
- Node.js 18+
- Git
- Claude Code CLI or IDE extension

## Architecture

```
├── .claude/agents/        # 4 base agent definitions
├── protocols/             # 18 behavioral protocols
│   ├── core/              # Routing, initialization, self-improvement
│   ├── agents/            # Agent creation, communication
│   ├── knowledge/         # Exploration, memory, context engineering
│   ├── quality/           # Testing, cloning, security
│   └── project/           # Project-specific (created at runtime)
├── memory/scripts/        # 9 Python scripts (Qdrant + Neo4j + fastembed)
├── mcp/                   # MCP server configurations
├── infra/                 # Docker Compose for Qdrant + Neo4j
├── docs/
│   ├── self-architecture/ # Pre-populated capability map + seed graph
│   └── [8 domains]/       # Design knowledge base (65+ reference docs)
├── secrets/               # .env (auto-generated by setup.sh)
├── CLAUDE.md              # Main coordinator instructions
└── setup.sh               # One-command setup
```

## How It Works

1. **setup.sh** installs dependencies, finds free ports, starts Docker containers, loads self-knowledge seed
2. **Claude Code** reads CLAUDE.md, detects `_WORKFLOW_NEEDS_INIT`, begins 11-phase initialization
3. **Phase 2 (Greet)** — coordinator introduces itself, you choose a code name
4. **Phase 3 (Explore)** — pathfinder scans YOUR project (not the workflow) and loads it into memory
5. **Phase 4 (Adapt)** — creates domain-specific agents based on your project archetype
6. **Phase 8 (Build-Up)** — validates self-knowledge, completes build-up cycle, version 0.2 → 1.0
7. From then on — persistent memory across sessions, self-improving, gap-aware

## Design Knowledge Base

The `docs/` directory contains 65+ reference documents across 8 domains that inform the coordinator's decisions:

| Domain | Focus |
|--------|-------|
| context-engineering | Context Rot, memory taxonomy, session architecture |
| claude-opus-4-6 | Model capabilities, T1-T5 query optimization |
| rag-graphrag | Hybrid RAG, GraphRAG, agentic retrieval |
| neo4j | Cypher, APOC, temporal graphs |
| embeddings-e5 | fastembed 384d, multilingual E5 |
| reranker-bge | BGE cross-encoder reranking |
| json-prompting | Structured output, Pydantic + Instructor |
| docker | Multi-container orchestration |

## License

MIT
