# Gap Analysis Log

Entries appended by gap-analysis protocol after each scan.

## Format

Each entry is a JSON block with fields: scan_type, timestamp, overall_score, gaps[], active_builds[], buffered_builds_relevant[], available_specs_matching[].

---
